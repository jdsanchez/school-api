# Ejemplo de variables de entorno para PRODUCCIÓN

# ============================================
# Backend - Express/Node.js
# ============================================

# Database Configuration (Usar credenciales de producción)
DB_HOST=tu-servidor-mysql.com
DB_USER=usuario_produccion
DB_PASSWORD=password_super_seguro_cambiar
DB_NAME=school_management
DB_PORT=3306

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Secret (CAMBIAR por una clave segura y única)
# Genera una con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambiar_por_clave_segura_de_al_menos_64_caracteres
JWT_EXPIRE=7d

# Frontend URL (URL donde está desplegado tu frontend)
FRONTEND_URL=https://tu-dominio.com

# Ejemplo con www:
# FRONTEND_URL=https://www.tu-dominio.com

# ============================================
# Email Configuration (para recuperación de contraseña)
# ============================================

# Opción 1: Gmail (recomendado para desarrollo y pequeña escala)
EMAIL_SERVICE=gmail
EMAIL_USER=tu-email@gmail.com
EMAIL_PASSWORD=tu-app-password-de-16-digitos
EMAIL_FROM=tu-email@gmail.com
EMAIL_FROM_NAME=Sistema Escolar

# Nota: Para Gmail, debes crear un "App Password" en:
# https://myaccount.google.com/apppasswords
# (Requiere autenticación de 2 factores activada)

# Opción 2: SMTP Genérico (para servicios como SendGrid, Mailgun, etc.)
# EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.tu-proveedor.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-usuario-smtp
# SMTP_PASSWORD=tu-password-smtp
# EMAIL_FROM=noreply@tu-dominio.com
# EMAIL_FROM_NAME=Sistema Escolar
